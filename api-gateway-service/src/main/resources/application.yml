spring:
  application:
    name: "api-gateway-service"
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
server:
  port: 8765

eureka:
  client:
    service-url:
      defaultZone: "http://localhost:8761/eureka"


